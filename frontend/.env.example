# Your backend runs on port 5000 (from server/.env PORT=5000)
# The proxy in package.json handles this automatically in development
# For production only, set this:
REACT_APP_API_URL=http://localhost:5000/api
